name: fvs_app
description: "La Sfida della Fede - Daily Saint Quote App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_mobile_ads: ^5.1.0
  flutter_local_notifications: ^17.0.0
  shared_preferences: ^2.2.0
  share_plus: ^7.0.0
  google_fonts: ^6.1.0
  intl: ^0.19.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/saints.json
